<%= link_to favorites_path(micropost_id: micropost.id), remote: true, method: :post, class: 'favorite-link' do %>
  <button type="button" class="btn btn-info btn-xs">
      <span class="glyphicon glyphicon-star-empty" aria-hidden="true">favorite</span>
  </button>
<% end %>