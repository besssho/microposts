<%= form_for(current_user.favorites.find_by(micropost_id: micropost.id),
             html: { method: :delete },
             remote: true) do |f| %>
<%= button_tag( class: "btn-warning btn-xs") do %>
  <%= content_tag :span,"お気に入り済", class: "glyphicon glyphicon-star" %>
  <% end %>
<% end %>